// TODO: Implement matching service
export const matchingService = {
  findMatches: async (clientId: string) => [],
  createConnection: async (clientId: string, advisorId: string) => null,
  getConnectionStatus: async (connectionId: string) => null
};